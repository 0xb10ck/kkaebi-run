class_name GameEnums
extends RefCounted

# 풀스펙 아키텍처 §3.1 — 공통 enum. 모든 데이터 리소스가 참조.

enum Element {
	NONE,
	FIRE,
	WATER,
	WOOD,
	METAL,
	EARTH,
}

enum Rarity {
	COMMON,
	RARE,
	LEGENDARY,
}

enum TriggerMode {
	AUTO,
	ACTIVE,
	PASSIVE,
	REACTIVE,
}

enum RangedKind {
	NONE,
	STRAIGHT,
	HOMING,
	ARC,
	AOE_DROP,
	CHANNEL_BEAM,
}

enum SpecialAbility {
	NONE,
	CHARGE,
	DRAG_SLOW,
	GAZE_GROWTH,
	GROUP_SWARM,
	SPAWN_MINIONS,
	PHASE_TELEPORT,
	INVISIBLE,
	EXPLODE_ON_DEATH,
	POISON_TRAIL,
	SHIELD_REGEN,
	ENRAGE_LOW_HP,
	HEALER,
	CALL_REINFORCEMENTS,
	FREEZE,
	KNOCKBACK_AURA,
	LIFESTEAL,
	REFLECT,
	PIERCE_SHIELD,
	STATUS_CURSE,
}

enum GroupAIKind {
	NONE,
	FLOCK_CENTER,
	LINE_FORMATION,
	CIRCLE_FORMATION,
	RANDOM_SCATTER,
	LEADER_FOLLOWERS,
}

enum BossPhaseTransition {
	HP_THRESHOLD,
	HP_AND_TIME,
	TIMED_ONLY,
	SCRIPTED,
}

enum PatternShape {
	CIRCLE_AOE,
	LINE_AOE,
	CONE_AOE,
	SCREEN_AOE,
	PROJECTILE_STRAIGHT,
	PROJECTILE_HOMING,
	PROJECTILE_BARRAGE,
	MELEE_LUNGE,
	SUMMON_MINIONS,
	BUFF_SELF,
	DEBUFF_PLAYER,
	GRAB,
}

enum EnvKind {
	THORNBUSH,
	FOG,
	POISON_MARSH,
	TALISMAN_PILLAR,
	SPIRIT_ALTAR,
}

enum EventKind {
	GOBLIN_MARKET,
	SPIRIT_BLESSING,
	DEMON_CURSE,
	TREASURE_CHEST,
	WANDERING_DOKKAEBI,
	BLOOD_MOON,
	INVISIBLE_CAP,
}
